apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gx-registry
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: gxdch-compliance
  sources:
    - repoURL: https://gitlab.com/gaia-x/lab/compliance/gx-registry.git
      targetRevision: main
      path: k8s/gx-registry
      helm:
        releaseName: main-registry
        valueFiles:
          - $values/platform-apps/gxdch-compliance/values/gx-registry-values.yaml
    - repoURL: https://github.com/REPLACE_ME/GXDCH-Compliance.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: gxdch-compliance
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
